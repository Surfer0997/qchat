import { createSlice } from "@reduxjs/toolkit";

export interface CurrentDialogue {
    
}

const currentDialogueSlice = createSlice({
    name: 'currentDialogue',
    initialState:{},
    reducers: {

    },

})